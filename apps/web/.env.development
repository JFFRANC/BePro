# 008-ux-roles-refinements / US8 — single-tenant deploy default for dev.
# Hides the "Organización" input on the login screen and submits this slug.
# To re-enable the multi-tenant input, set this to an empty string in a
# .env.local override.
VITE_LOGIN_TENANT_FIXED=bepro
