# 008-ux-roles-refinements / US8 — single-tenant production default.
# Hides the "Organización" input on the login screen and submits this slug.
# Override at deploy time (Cloudflare Pages env vars) for multi-tenant SaaS.
VITE_LOGIN_TENANT_FIXED=bepro
