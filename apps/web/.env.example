# BePro Web — environment variables
# Copy to .env.local for local development.

# Hide the tenant/organization input on the login screen and submit this value.
# Set to empty string to re-enable the input (for multi-tenant public sign-in).
VITE_LOGIN_TENANT_FIXED=bepro
